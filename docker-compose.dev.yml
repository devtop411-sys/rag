services:
  backend:
    environment:
      PORT: 3011

  mcp:
    environment:
      MCP_PORT: 3012

  frontend:
    build:
      args:
        VITE_API_URL: ${VITE_API_URL:-https://dev.rag.collider.vc}
