PORT=3001

VOYAGE_API_KEY=your_voyage_api_key
VOYAGE_MODEL=voyage-4-lite

# Voyage rate limits — the ceiling the embeddings client paces itself to.
# Defaults (2000 RPM / 16M TPM) match usage tier 1 for voyage-4-lite, i.e. any
# account with a payment method. The client halves its rate on a 429 and eases
# back up afterwards, so a free-trial account (3 RPM / 10K TPM) self-corrects
# without configuration. Set these only to impose a lower ceiling.
# VOYAGE_MAX_RPM=2000
# VOYAGE_MAX_TPM=16000000

# ---------------------------------------------------------------------------
# AWS S3 — required for file storage
# ---------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
S3_BUCKET=your-s3-bucket-name

QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=investment_memos

# Optional — set a shared secret to protect the API endpoints.
# Must be the same value as VITE_API_KEY in the frontend .env.
API_KEY=

# ---------------------------------------------------------------------------
# Frontend (Vite)
# ---------------------------------------------------------------------------
# Optional — override the MCP Playground default endpoint.
# Defaults to same-origin /mcp (e.g. https://rag.collider.vc/mcp).
VITE_MCP_URL=

# ---------------------------------------------------------------------------
# MCP server (Streamable HTTP transport, exposed at /mcp)
# Secured with OAuth 2.1 + PKCE. Clients (e.g. claude.ai Custom Connectors)
# must complete a Google sign-in before they can call any tool.
# ---------------------------------------------------------------------------
MCP_PORT=3002

# Public origin of this server, used in OAuth metadata and token audiences.
# MUST be set in production so token issuer/audience are stable behind proxies.
# e.g. https://rag.collider.vc
PUBLIC_BASE_URL=

# Secret used to sign MCP access/refresh tokens (JWT HS256). REQUIRED in
# production — if unset, an ephemeral key is generated and tokens are invalidated
# on every restart and won't validate across multiple instances.
# Generate one with:  node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
OAUTH_SIGNING_SECRET=

# Token lifetimes in seconds (optional). Defaults: 1h access, 30d refresh.
OAUTH_ACCESS_TTL=3600
OAUTH_REFRESH_TTL=2592000

# Extra allowed OAuth redirect URIs (comma-separated). claude.ai / claude.com
# callbacks and localhost are always allowed; add others only if needed.
OAUTH_EXTRA_REDIRECT_URIS=

# ---------------------------------------------------------------------------
# Meta-query tagging (optional)
# When set, each ingested chunk is labelled with financial topic tags via LLM.
# Any OpenAI-compatible endpoint works (OpenAI, Azure, local Ollama, etc.).
# ---------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1

# Parallel LLM calls during ingestion. This is the slowest part of ingesting a
# document (two calls per chunk), so raise it if your OpenAI tier allows and
# lower it if you start seeing 429s. Defaults to 12.
# OPENAI_MAX_CONCURRENCY=12

# ---------------------------------------------------------------------------
# Slack bot + Dify
# ---------------------------------------------------------------------------
SLACK_BOT_TOKEN=xoxb-...
DIFY_API_KEY=
DIFY_URL=http://34.245.224.130/v1/chat-messages

# ---------------------------------------------------------------------------
# Fireflies.ai connector (meeting transcripts → RAG)
# ---------------------------------------------------------------------------
# Optional fallback API key. Normally the key is entered in the UI (Fireflies
# tab) and persisted; this env var is only used when nothing has been saved.
# Get a key at: https://app.fireflies.ai/settings (Developer Settings)
FIREFLIES_API_KEY=

# Optional — override the Qdrant collection used to store connector state
# (API key, sync cursor, auto-sync settings). Defaults to "fireflies_state".
FIREFLIES_STATE_COLLECTION=fireflies_state

# Max meetings a single auto-sync run will ingest. Keeps a run short enough to
# finish and commit its cursor at the Voyage free-tier embedding pace; leftovers
# are picked up by the next run (which is scheduled ~5 min later while a
# backlog remains). Defaults to 5.
FIREFLIES_MAX_INGESTS_PER_RUN=5

# ---------------------------------------------------------------------------
# Google OAuth — restrict login to @collider.vc accounts
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Authorised JavaScript origins: https://your-domain.com
#   (MUST include PUBLIC_BASE_URL — the MCP sign-in page renders the Google
#    button on this server's own domain.)
# ---------------------------------------------------------------------------
GOOGLE_CLIENT_ID=
# Alias accepted by the backend (same value as GOOGLE_CLIENT_ID):
# MCP_GOOGLE_CLIENT_ID=

# Client secret for the same Web application OAuth client.
# Required for Google Drive permanent connection (auth-code + refresh token).
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_SECRET=
# Alias accepted by the backend:
# MCP_GOOGLE_CLIENT_SECRET=

# ---------------------------------------------------------------------------
# Google Drive connector (browse/ingest files + background auto-ingest)
# Reuses GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET above (or MCP_GOOGLE_* aliases).
# Enable Google Drive API: https://console.cloud.google.com/apis/library/drive.googleapis.com
# Authorised JavaScript origins must include the frontend origin.
# Scope used: drive.readonly. Refresh tokens keep auto-ingest alive 24/7.
# ---------------------------------------------------------------------------
VITE_GOOGLE_DRIVE_CLIENT_ID=
